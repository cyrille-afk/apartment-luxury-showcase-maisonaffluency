# Lock Gallery to Four Columns

## Changes
- Remove the desktop gallery density toggle controls beside “A Sociable Environment”.
- Replace the variable desktop grid with a permanent four-column layout.
- Keep the existing 1500px header-aligned container boundary and current mobile presentation unchanged.
- Remove obsolete desktop grid-state logic and conditional card behavior tied to the old controls.

## Verification
- Confirm exactly four cards render across on desktop within the header boundaries.
- Confirm no layout controls remain and the page has no build or browser errors.
