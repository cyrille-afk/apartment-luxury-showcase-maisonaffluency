name: Mobile PWA & a11y checks

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  vitest-unit:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    # Guards PDF/quote business logic — including the 100%-upfront flip when
    # stock_status_override or lead_weeks_max_override changes (see
    # src/lib/__tests__/computeDepositPct.test.ts).
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
      - run: npm ci --prefer-offline --no-audit --no-fund
      - name: Run vitest unit suite
        run: npx vitest run

  playwright-mobile:
    runs-on: ubuntu-latest
    container:
      image: mcr.microsoft.com/playwright:v1.60.0-noble
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
      # Manual npm cache — actions/setup-node's cache doesn't apply inside a container job.
      - name: Cache npm
        uses: actions/cache@v4
        with:
          path: |
            ~/.npm
            ~/.cache/ms-playwright
          key: mobile-playwright-${{ runner.os }}-${{ hashFiles('package-lock.json') }}
          restore-keys: mobile-playwright-${{ runner.os }}-
      - run: npm ci --prefer-offline --no-audit --no-fund
      - name: Ensure Chromium is installed
        run: npx playwright install chromium
      - name: Run mobile Playwright checks
        shell: bash
        run: |
          set -o pipefail
          npm run test:e2e:mobile-pwa 2>&1 | tee playwright-mobile.log
        timeout-minutes: 10
        env:
          # Verbose Playwright API logs surface in the GH Action output, making
          # CDP-disconnect / timeout failures debuggable without re-running locally.
          DEBUG: pw:api
      - name: Upload Playwright report
        if: failure() || cancelled()
        uses: actions/upload-artifact@v5
        with:
          name: playwright-report
          path: |
            playwright-report
            test-results
            playwright-mobile.log
          retention-days: 14
          if-no-files-found: ignore

  playwright-visual:
    runs-on: ubuntu-latest
    container:
      image: mcr.microsoft.com/playwright:v1.60.0-noble
    timeout-minutes: 20
    # Visual regression for landing CTAs / hero / designers section across mobile + desktop.
    # Baselines live under e2e/visual/__screenshots__/. Refresh locally with `npm run test:visual:update`.
    steps:
      - uses: actions/checkout@v5
      - name: Cache npm
        uses: actions/cache@v4
        with:
          path: |
            ~/.npm
            ~/.cache/ms-playwright
          key: visual-playwright-${{ runner.os }}-${{ hashFiles('package-lock.json') }}
          restore-keys: visual-playwright-${{ runner.os }}-
      - run: npm ci --prefer-offline --no-audit --no-fund
      - name: Ensure Chromium is installed
        run: npx playwright install chromium
      - name: Run visual structural checks (blocking)
        shell: bash
        run: |
          set -o pipefail
          npm run test:visual -- --grep-invert '@pixel' 2>&1 | tee playwright-visual-structural.log
        timeout-minutes: 10
      - name: Run pixel snapshot checks (informational)
        shell: bash
        continue-on-error: true
        run: |
          set -o pipefail
          npm run test:visual -- --grep '@pixel' 2>&1 | tee playwright-visual-pixel.log
        timeout-minutes: 10
      - name: Upload visual report
        if: failure() || cancelled()
        uses: actions/upload-artifact@v5
        with:
          name: playwright-visual-report
          path: |
            playwright-report
            test-results
            playwright-visual-structural.log
            playwright-visual-pixel.log
          retention-days: 14
          if-no-files-found: ignore

  lighthouse-mobile:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    # Blocking mobile performance gate: median of 2 Lighthouse runs on "/" under
    # simulated mobile 4G. Fails the build when FCP / LCP / CLS / TBT or the
    # performance / accessibility scores breach the budgets in
    # scripts/lighthouse-mobile.mjs. Scope is deliberately narrow — the previous
    # 2-route × 3-run matrix overran the 35-minute step budget on shared runners.
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm ci --prefer-offline --no-audit --no-fund
      - name: Run mobile Lighthouse routes (blocking)
        shell: bash
        run: |
          set -o pipefail
          ROUTES="/" \
          LH_RUNS=2 \
          LH_THROTTLING=simulate \
          LH_AUDIT_TIMEOUT_MS=120000 \
          npm run lighthouse:mobile 2>&1 | tee lighthouse-mobile.log
        timeout-minutes: 18
      - name: Summarise Core Web Vitals
        if: always()
        shell: bash
        run: |
          {
            echo "### Lighthouse mobile (median of 3 runs)"
            echo '```'
            grep -E '^(▸|  [✓✗])' lighthouse-mobile.log || echo "no metrics captured"
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Upload Lighthouse report
        if: always()
        uses: actions/upload-artifact@v5
        with:
          name: lighthouse-report
          path: |
            lighthouse-report
            lighthouse-mobile.log
          retention-days: 14
          if-no-files-found: ignore
